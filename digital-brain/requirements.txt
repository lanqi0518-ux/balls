fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
numpy>=1.26.0
torch>=2.2.0
# Playwright drives the real headless Chromium the "web embodiment"
# subsystem uses (Phase 2). Pillow downsamples screenshots for streaming.
playwright>=1.47.0
pillow>=10.0.0
# Live Solana execution: solders gives us pure-Rust Keypair / VersionedTx
# handling, httpx does async HTTP to Jupiter Ultra + Solana RPC.
solders>=0.21.0
httpx>=0.27.0
base58>=2.1.1
# Live Robinhood-Chain execution: eth-account + eth-abi give us a
# secp256k1 signer + Uniswap-V3 calldata encoder without pulling in the
# full 8 MB web3.py stack. RPC calls go over httpx (already present).
eth-account>=0.13.0
eth-abi>=5.1.0
